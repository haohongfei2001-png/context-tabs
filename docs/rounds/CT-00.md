# CT-00 — Product/API Freeze

Status: COMPLETE

## Authorized scope

Design and repository bootstrap only. No extension runtime code.

## Required outputs

- product intent and v0.1 boundary
- Chrome-as-operational-truth model
- Side Panel surface choice
- `+` and `AI+` semantics
- permission envelope
- failure/fallback invariants
- development rounds
- execution protocol and canonical status

## Acceptance

CT-00 passes when the documents are internally consistent and explicitly prevent New Tab hijacking, native-strip patching, premature ChatGPT DOM injection, telemetry, and proprietary workspace state in v0.1.

## Closure

The GitHub repository exists and remote `main` contains the frozen product/API specification. CT-01 may begin only as a separate execution.
